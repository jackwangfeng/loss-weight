<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 9+ (API 28+) blocks cleartext HTTP by default. Prod now goes through
  Cloudflare (https://recompdaily.com) so the beta EC2 IP exception is gone;
  only localhost / emulator loopback stay cleartext-allowed for local dev.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- 10.0.2.2 is the Android emulator's loopback to the host machine;
             localhost is the on-device path for tethered dev builds. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
